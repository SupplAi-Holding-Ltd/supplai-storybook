import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundation/Colors" />

<div className="cp-page">

  <div className="cp-header">
    <h1>Colors</h1>
    <p>The Supplai color system is built for clarity, accessibility, and adaptability. It supports both light and dark surfaces, communicates semantic meaning through consistent hue choices, and scales across products and contexts. All colors are tokenized for systematic use in design and code.</p>
  </div>

  {/* ─── Brand Colors ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Brand Colors</h2>
    <p className="cp-description">The following colors make up the primary thinking for the brand identity. These should be considered as the signature colors across all Supplai products.</p>

    <div className="cp-brand-hex-row">
      {[
        { hex: "#000000", label: "Black" },
        { hex: "#0D1B2A", label: "Dark Navy" },
        { hex: "#2D9C93", label: "Teal" },
        { hex: "#4169E1", label: "Brand Blue" },
        { hex: "#FFFFFF", label: "White", outline: true },
      ].map(({ hex, label, outline }) => (
        <div key={hex} className="cp-hex-wrap">
          <div className="cp-hexagon" style={{ background: hex, border: outline ? "2px solid #D1D5DB" : "none" }} />
          <span className="cp-hex-label">{label}</span>
        </div>
      ))}
    </div>

    <div className="cp-brand-large-row">
      {[
        { hex: "#000000", label: "Black", sub: "#000000" },
        { hex: "#0D1B2A", label: "MidNight", sub: "#0D1B2A" },
        { hex: "#F4F6FE", label: "White Blue", sub: "#F4F6FE", dark: true },
      ].map(({ hex, label, sub, dark }) => (
        <div key={hex} className="cp-brand-large-card" style={{ background: hex }}>
          <span className="cp-brand-large-name" style={{ color: dark ? "#111827" : "#FFFFFF" }}>{label}</span>
          <span className="cp-brand-large-hex" style={{ color: dark ? "#6B7280" : "rgba(255,255,255,0.6)" }}>{sub}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Blue Palette ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#0D1E5C" },
        { shade: "800", hex: "#162478" },
        { shade: "700", hex: "#1C2E99" },
        { shade: "600", hex: "#2B44BF" },
        { shade: "500", hex: "#4169E1", brand: true },
        { shade: "400", hex: "#6688E9" },
        { shade: "300", hex: "#8EAAF1" },
        { shade: "200", hex: "#BCCDF8" },
        { shade: "100", hex: "#E8EEFB" },
        { shade: "50",  hex: "#F4F7FE" },
      ].map(({ shade, hex, brand }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex, outline: brand ? "2px solid #4169E1" : "none", outlineOffset: "2px" }} />
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
          {brand && <span className="cp-swatch-tag">Brand</span>}
        </div>
      ))}
    </div>
  </div>

  {/* ─── Teal Palette ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#0D3330" },
        { shade: "800", hex: "#134D49" },
        { shade: "700", hex: "#196861" },
        { shade: "600", hex: "#208078" },
        { shade: "500", hex: "#2D9C93" },
        { shade: "400", hex: "#50B5AD" },
        { shade: "300", hex: "#7ECDC7" },
        { shade: "200", hex: "#B3E5E2" },
        { shade: "100", hex: "#E0F5F4" },
        { shade: "50",  hex: "#F2FBFA" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }} />
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Cyan / Sky Palette ───────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#082F49" },
        { shade: "800", hex: "#0C4A6E" },
        { shade: "700", hex: "#0369A1" },
        { shade: "600", hex: "#0284C7" },
        { shade: "500", hex: "#0EA5E9" },
        { shade: "400", hex: "#38BDF8" },
        { shade: "300", hex: "#7DD3FC" },
        { shade: "200", hex: "#BAE6FD" },
        { shade: "100", hex: "#E0F2FE" },
        { shade: "50",  hex: "#F0F9FF" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }} />
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Semantic Colors ──────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Semantic Colors</h2>
    <p className="cp-description">These colors communicate meaning and status throughout the interface. They should be used for feedback, alerts, and state indication — not for decoration.</p>

    <div className="cp-semantic-icons">
      <div className="cp-semantic-hex" style={{ background: "#F59E0B" }} />
      <div className="cp-semantic-hex" style={{ background: "#EF4444" }} />
    </div>
  </div>

  {/* ─── Amber / Orange Palette ───────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#431407" },
        { shade: "800", hex: "#7A2D0F" },
        { shade: "700", hex: "#B45309" },
        { shade: "600", hex: "#D97706" },
        { shade: "500", hex: "#F59E0B" },
        { shade: "400", hex: "#FBBF24" },
        { shade: "300", hex: "#FCD34D" },
        { shade: "200", hex: "#FDE68A" },
        { shade: "100", hex: "#FEF3C7" },
        { shade: "50",  hex: "#FFFBEB" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }} />
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Red / Error Palette ──────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#450A0A" },
        { shade: "800", hex: "#7F1D1D" },
        { shade: "700", hex: "#991B1B" },
        { shade: "600", hex: "#DC2626" },
        { shade: "500", hex: "#EF4444" },
        { shade: "400", hex: "#F87171" },
        { shade: "300", hex: "#FCA5A5" },
        { shade: "200", hex: "#FECACA" },
        { shade: "100", hex: "#FEE2E2" },
        { shade: "50",  hex: "#FFF5F5" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }} />
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Grey Palette ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Grey Palette</h2>
    <p className="cp-description">A cool blue-tinted grey scale used for surfaces, borders, text, and backgrounds. These greys are designed to pair naturally with the brand blue.</p>
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#0F172A" },
        { shade: "800", hex: "#1E293B" },
        { shade: "700", hex: "#334155" },
        { shade: "600", hex: "#475569" },
        { shade: "500", hex: "#64748B" },
        { shade: "400", hex: "#94A3B8" },
        { shade: "300", hex: "#CBD5E1" },
        { shade: "200", hex: "#E2E8F0" },
        { shade: "100", hex: "#F1F5F9" },
        { shade: "50",  hex: "#F8FAFC" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }} />
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Dark Surface Colors ──────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Dark Surface Colors</h2>
    <p className="cp-description">These are the designated surface colors for dark mode interfaces. They create depth through subtle layering without relying on pure black.</p>
    <div className="cp-dark-surfaces">
      <div className="cp-dark-card" style={{ background: "#0D0F13", flex: 2 }}>
        <div className="cp-dark-label-group">
          <span className="cp-dark-name">Surface</span>
          <span className="cp-dark-hex">#0D0F13</span>
        </div>
      </div>
      <div style={{ flex: 1, display: "flex", flexDirection: "column", gap: "8px" }}>
        <div className="cp-dark-card" style={{ background: "#1C2136", flex: 1 }}>
          <div className="cp-dark-label-group">
            <span className="cp-dark-name">Surface 02</span>
            <span className="cp-dark-hex">#1C2136</span>
          </div>
        </div>
        <div className="cp-dark-card" style={{ background: "rgba(65,105,225,0.35)", flex: 1, border: "1px solid rgba(65,105,225,0.4)" }}>
          <div className="cp-dark-label-group">
            <span className="cp-dark-name">Accent 35%</span>
            <span className="cp-dark-hex">#4169E1 · 35%</span>
          </div>
        </div>
        <div className="cp-dark-card" style={{ background: "rgba(65,105,225,0.15)", flex: 1, border: "1px solid rgba(65,105,225,0.25)" }}>
          <div className="cp-dark-label-group">
            <span className="cp-dark-name">Accent 15%</span>
            <span className="cp-dark-hex">#4169E1 · 15%</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  {/* ─── Color Tokens ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Color Tokens</h2>
    <p className="cp-description">Colors are used in the design system through semantic tokens — not raw hex values. Tokens map to specific roles (background, text, border, etc.) and adapt automatically between light and dark modes.</p>
    <div className="cp-tokens-grid">
      {[
        { token: "--color-bg-primary",      value: "#FFFFFF",  label: "Background / Primary",   dark: false },
        { token: "--color-bg-secondary",    value: "#F4F6FE",  label: "Background / Secondary",  dark: false },
        { token: "--color-bg-inverse",      value: "#0D0F13",  label: "Background / Inverse",    dark: true  },
        { token: "--color-text-primary",    value: "#0F172A",  label: "Text / Primary",          dark: false },
        { token: "--color-text-secondary",  value: "#64748B",  label: "Text / Secondary",        dark: false },
        { token: "--color-text-inverse",    value: "#FFFFFF",  label: "Text / Inverse",          dark: true  },
        { token: "--color-brand",           value: "#4169E1",  label: "Brand / Default",         dark: true  },
        { token: "--color-border-default",  value: "#E2E8F0",  label: "Border / Default",        dark: false },
        { token: "--color-border-strong",   value: "#94A3B8",  label: "Border / Strong",         dark: false },
        { token: "--color-success",         value: "#2D9C93",  label: "Semantic / Success",      dark: true  },
        { token: "--color-warning",         value: "#F59E0B",  label: "Semantic / Warning",      dark: false },
        { token: "--color-error",           value: "#EF4444",  label: "Semantic / Error",        dark: true  },
      ].map(({ token, value, label, dark }) => (
        <div key={token} className="cp-token-row">
          <div className="cp-token-swatch" style={{ background: value, border: dark ? "none" : "1px solid #E5E7EB" }} />
          <div className="cp-token-info">
            <span className="cp-token-label">{label}</span>
            <code className="cp-token-name">{token}</code>
          </div>
          <span className="cp-token-value">{value}</span>
        </div>
      ))}
    </div>
  </div>

</div>

<style>
  {`
  .cp-page {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 0 64px;
    font-family: inherit;
  }

  .cp-header {
    margin-bottom: 48px;
    padding-bottom: 32px;
    border-bottom: 1px solid #E5E7EB;
  }

  .cp-header h1 {
    font-size: 32px;
    font-weight: 700;
    color: #111827;
    margin: 0 0 12px;
  }

  .cp-header p {
    font-size: 15px;
    color: #6B7280;
    line-height: 1.7;
    max-width: 680px;
    margin: 0;
  }

  .cp-section {
    margin-bottom: 40px;
  }

  .cp-section h2 {
    font-size: 18px;
    font-weight: 700;
    color: #111827;
    margin: 0 0 6px;
  }

  .cp-description {
    font-size: 13px;
    color: #6B7280;
    line-height: 1.7;
    margin: 0 0 20px;
    max-width: 680px;
  }

  /* ── Brand hex row ── */
  .cp-brand-hex-row {
    display: flex;
    gap: 16px;
    margin-bottom: 20px;
    align-items: flex-end;
  }

  .cp-hex-wrap {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .cp-hexagon {
    width: 40px;
    height: 46px;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
    flex-shrink: 0;
  }

  .cp-hex-label {
    font-size: 10px;
    color: #9CA3AF;
    text-align: center;
    white-space: nowrap;
  }

  /* ── Brand large cards ── */
  .cp-brand-large-row {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 12px;
  }

  .cp-brand-large-card {
    border-radius: 10px;
    height: 80px;
    padding: 12px 16px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    border: 1px solid rgba(0,0,0,0.06);
  }

  .cp-brand-large-name {
    font-size: 13px;
    font-weight: 600;
  }

  .cp-brand-large-hex {
    font-size: 11px;
    font-family: monospace;
  }

  /* ── Palette row ── */
  .cp-palette-row {
    display: grid;
    grid-template-columns: repeat(10, 1fr);
    gap: 6px;
  }

  .cp-swatch-wrap {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .cp-swatch {
    width: 100%;
    aspect-ratio: 1 / 1.4;
    border-radius: 8px;
    border: 1px solid rgba(0,0,0,0.04);
    min-height: 56px;
  }

  .cp-swatch-shade {
    font-size: 10px;
    font-weight: 600;
    color: #374151;
  }

  .cp-swatch-hex {
    font-size: 9px;
    color: #9CA3AF;
    font-family: monospace;
    word-break: break-all;
  }

  .cp-swatch-tag {
    font-size: 9px;
    font-weight: 600;
    color: #4169E1;
    background: #E8EEFB;
    padding: 1px 4px;
    border-radius: 3px;
    width: fit-content;
  }

  /* ── Semantic icons ── */
  .cp-semantic-icons {
    display: flex;
    gap: 12px;
    margin-bottom: 20px;
  }

  .cp-semantic-hex {
    width: 48px;
    height: 55px;
    clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
  }

  /* ── Dark surfaces ── */
  .cp-dark-surfaces {
    display: flex;
    gap: 12px;
    height: 180px;
  }

  .cp-dark-card {
    border-radius: 12px;
    padding: 16px;
    display: flex;
    align-items: flex-end;
  }

  .cp-dark-label-group {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .cp-dark-name {
    font-size: 12px;
    font-weight: 600;
    color: rgba(255,255,255,0.8);
  }

  .cp-dark-hex {
    font-size: 10px;
    color: rgba(255,255,255,0.45);
    font-family: monospace;
  }

  /* ── Tokens ── */
  .cp-tokens-grid {
    display: flex;
    flex-direction: column;
    border: 1px solid #E5E7EB;
    border-radius: 10px;
    overflow: hidden;
  }

  .cp-token-row {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 10px 16px;
    border-bottom: 1px solid #F3F4F6;
    background: #FFFFFF;
  }

  .cp-token-row:last-child {
    border-bottom: none;
  }

  .cp-token-row:nth-child(even) {
    background: #FAFAFA;
  }

  .cp-token-swatch {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    flex-shrink: 0;
  }

  .cp-token-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .cp-token-label {
    font-size: 13px;
    font-weight: 500;
    color: #111827;
  }

  .cp-token-name {
    font-size: 11px;
    color: #6B7280;
    font-family: monospace;
    background: #F3F4F6;
    padding: 1px 5px;
    border-radius: 3px;
    width: fit-content;
  }

  .cp-token-value {
    font-size: 11px;
    color: #9CA3AF;
    font-family: monospace;
    flex-shrink: 0;
  }

  @media (max-width: 700px) {
    .cp-palette-row {
      grid-template-columns: repeat(5, 1fr);
    }
    .cp-brand-large-row {
      grid-template-columns: 1fr;
    }
    .cp-dark-surfaces {
      flex-direction: column;
      height: auto;
    }
    .cp-dark-card {
      min-height: 80px;
    }
  }
  `}
</style>
