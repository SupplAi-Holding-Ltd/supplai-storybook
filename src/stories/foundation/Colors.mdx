import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundation/Colors" />

<div className="cp-page">

  <div className="cp-header">
    <h1>Colors</h1>
    <p>The supplai color system is built for clarity, accessibility, and adaptability. It supports both light and dark surfaces, communicates semantic meaning through consistent hue choices, and scales across products and contexts. All colors are tokenized for systematic use in design and code.</p>
  </div>

  {/* ─── Brand Colors ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Brand Colors</h2>
    <p className="cp-description">The following colors make up the primary thinking for the brand identity. These should be considered as the signature colors across all supplai products.</p>

    <div className="cp-brand-hex-row">
      {[
        { hex: "#4169E1", label: "Brand Blue" },
        { hex: "#0D1B2A", label: "Dark Navy" },
        { hex: "#2D9C93", label: "Teal" },
        { hex: "#000000", label: "Black" },
        { hex: "#FFFFFF", label: "White", outline: true },
      ].map(({ hex, label, outline }) => (
        <div key={hex} className="cp-hex-wrap">
          {/* Rounded hex (~4px); SVG so corners + stroke render (clip-path clips borders) */}
          <svg className="cp-hexagon" width="40" height="46" viewBox="-1.5 -1.5 43 49" aria-hidden="true">
            <path
              d="M 16.53 1.99 Q 20.00 0.00 23.47 1.99 L 36.53 9.51 Q 40.00 11.50 40.00 15.50 L 40.00 30.50 Q 40.00 34.50 36.53 36.49 L 23.47 44.01 Q 20.00 46.00 16.53 44.01 L 3.47 36.49 Q 0.00 34.50 0.00 30.50 L 0.00 15.50 Q 0.00 11.50 3.47 9.51 Z"
              fill={hex}
              stroke={outline ? "#F1F5F9" : "none"}
              strokeWidth={outline ? 1.5 : 0}
            />
          </svg>
          <span className="cp-hex-label">{label}</span>
        </div>
      ))}
    </div>

    <div className="cp-brand-large-row">
      {[
        { hex: "#000000", label: "Black", sub: "#000000" },
        { hex: "#0D1B2A", label: "MidNight", sub: "#0D1B2A" },
        { hex: "#F4F6FE", label: "White Blue", sub: "#F4F6FE", dark: true },
      ].map(({ hex, label, sub, dark }) => (
        <div key={hex} className="cp-brand-large-card" style={{ background: hex }}>
          <button className="cp-copy-btn" onClick={(e) => { navigator.clipboard.writeText(hex); const b = e.currentTarget; b.classList.add('cp-copy-btn--copied'); setTimeout(() => b.classList.remove('cp-copy-btn--copied'), 1500); }} title={`Copy ${hex}`}>
            <svg className="cp-copy-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" strokeWidth="1.5"/><path d="M3 11V3a1 1 0 0 1 1-1h8" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg>
            <svg className="cp-check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8l4 4 6-6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
          </button>
          <span className="cp-brand-large-name" style={{ color: dark ? "#111827" : "#FFFFFF" }}>{label}</span>
          <span className="cp-brand-large-hex" style={{ color: dark ? "#6B7280" : "rgba(255,255,255,0.6)" }}>{sub}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Blue Palette ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#142782" },
        { shade: "800", hex: "#203AA2" },
        { shade: "700", hex: "#2F50C1" },
        { shade: "600", hex: "#4169E1", brand: true },
        { shade: "500", hex: "#6787E7" },
        { shade: "400", hex: "#8DA5ED" },
        { shade: "300", hex: "#B3C3F3" },
        { shade: "200", hex: "#D9E1F9" },
        { shade: "100", hex: "#ECF0FC" },
        { shade: "50",  hex: "#F5F7FD" },
      ].map(({ shade, hex, brand }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex, outline: brand ? "2px solid #4169E1" : "none", outlineOffset: "2px" }}>
            <button className="cp-copy-btn" onClick={(e) => { navigator.clipboard.writeText(hex); const b = e.currentTarget; b.classList.add('cp-copy-btn--copied'); setTimeout(() => b.classList.remove('cp-copy-btn--copied'), 1500); }} title={`Copy ${hex}`}>
              <svg className="cp-copy-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" strokeWidth="1.5"/><path d="M3 11V3a1 1 0 0 1 1-1h8" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg>
              <svg className="cp-check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8l4 4 6-6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
            </button>
          </div>
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
          {brand && <span className="cp-swatch-tag">Brand</span>}
        </div>
      ))}
    </div>
  </div>

  {/* ─── Green Palette ────────────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#19595B" },
        { shade: "800", hex: "#28716C" },
        { shade: "700", hex: "#3B877B" },
        { shade: "600", hex: "#519E8A" },
        { shade: "500", hex: "#74B1A1" },
        { shade: "400", hex: "#97C5B9" },
        { shade: "300", hex: "#B9D8D0" },
        { shade: "200", hex: "#DCECE8" },
        { shade: "100", hex: "#EEF6F4" },
        { shade: "50",  hex: "#F6FAF9" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }}>
            <button className="cp-copy-btn" onClick={(e) => { navigator.clipboard.writeText(hex); const b = e.currentTarget; b.classList.add('cp-copy-btn--copied'); setTimeout(() => b.classList.remove('cp-copy-btn--copied'), 1500); }} title={`Copy ${hex}`}>
              <svg className="cp-copy-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" strokeWidth="1.5"/><path d="M3 11V3a1 1 0 0 1 1-1h8" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg>
              <svg className="cp-check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8l4 4 6-6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
            </button>
          </div>
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Teal Palette ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#30788A" },
        { shade: "800", hex: "#4CA1AC" },
        { shade: "700", hex: "#6ECBCD" },
        { shade: "600", hex: "#97EFE9" },
        { shade: "500", hex: "#ACF2ED" },
        { shade: "400", hex: "#C1F5F2" },
        { shade: "300", hex: "#D5F9F6" },
        { shade: "200", hex: "#EAFCFB" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }}>
            <button className="cp-copy-btn" onClick={(e) => { navigator.clipboard.writeText(hex); const b = e.currentTarget; b.classList.add('cp-copy-btn--copied'); setTimeout(() => b.classList.remove('cp-copy-btn--copied'), 1500); }} title={`Copy ${hex}`}>
              <svg className="cp-copy-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" strokeWidth="1.5"/><path d="M3 11V3a1 1 0 0 1 1-1h8" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg>
              <svg className="cp-check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8l4 4 6-6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
            </button>
          </div>
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Semantic Colors ──────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Semantic Colors</h2>
    <p className="cp-description">These colors communicate meaning and status throughout the interface. They should be used for feedback, alerts, and state indication — not for decoration.</p>

    <div className="cp-semantic-icons">
      {["#FF9F05", "#D13145"].map((hex) => (
        <svg key={hex} className="cp-semantic-hex" width="48" height="55" viewBox="0 0 48 55" aria-hidden="true">
          <path
            d="M 20.53 1.99 Q 24.00 0.00 27.47 1.99 L 44.53 11.76 Q 48.00 13.75 48.00 17.75 L 48.00 37.25 Q 48.00 41.25 44.53 43.24 L 27.47 53.01 Q 24.00 55.00 20.53 53.01 L 3.47 43.24 Q 0.00 41.25 0.00 37.25 L 0.00 17.75 Q 0.00 13.75 3.47 11.76 Z"
            fill={hex}
          />
        </svg>
      ))}
    </div>
  </div>

  {/* ─── Amber / Orange Palette ───────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#934A01" },
        { shade: "800", hex: "#B76302" },
        { shade: "700", hex: "#DB8003" },
        { shade: "600", hex: "#FF9F05", base: true },
        { shade: "500", hex: "#FFB237" },
        { shade: "400", hex: "#FFC569" },
        { shade: "300", hex: "#FFD99B" },
        { shade: "200", hex: "#FFECCD" },
        { shade: "100", hex: "#FFF6E6" },
        { shade: "50",  hex: "#FFFAF2" },
      ].map(({ shade, hex, base }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex, outline: base ? "2px solid #FF9F05" : "none", outlineOffset: "2px" }}>
            <button className="cp-copy-btn" onClick={(e) => { navigator.clipboard.writeText(hex); const b = e.currentTarget; b.classList.add('cp-copy-btn--copied'); setTimeout(() => b.classList.remove('cp-copy-btn--copied'), 1500); }} title={`Copy ${hex}`}>
              <svg className="cp-copy-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" strokeWidth="1.5"/><path d="M3 11V3a1 1 0 0 1 1-1h8" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg>
              <svg className="cp-check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8l4 4 6-6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
            </button>
          </div>
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Red / Error Palette ──────────────────────────────────────── */}
  <div className="cp-section">
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#790F3A" },
        { shade: "800", hex: "#96183F" },
        { shade: "700", hex: "#B32343" },
        { shade: "600", hex: "#D13145", base: true },
        { shade: "500", hex: "#DA5A6A" },
        { shade: "400", hex: "#E3838F" },
        { shade: "300", hex: "#EDADB5" },
        { shade: "200", hex: "#F6D6DA" },
        { shade: "100", hex: "#FBEBED" },
        { shade: "50",  hex: "#FCF4F5" },
      ].map(({ shade, hex, base }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex, outline: base ? "2px solid #D13145" : "none", outlineOffset: "2px" }}>
            <button className="cp-copy-btn" onClick={(e) => { navigator.clipboard.writeText(hex); const b = e.currentTarget; b.classList.add('cp-copy-btn--copied'); setTimeout(() => b.classList.remove('cp-copy-btn--copied'), 1500); }} title={`Copy ${hex}`}>
              <svg className="cp-copy-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" strokeWidth="1.5"/><path d="M3 11V3a1 1 0 0 1 1-1h8" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg>
              <svg className="cp-check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8l4 4 6-6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
            </button>
          </div>
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Grey Palette ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Grey Palette</h2>
    <p className="cp-description">A cool blue-tinted grey scale used for surfaces, borders, text, and backgrounds. These greys are designed to pair naturally with the brand blue.</p>
    <div className="cp-palette-row">
      {[
        { shade: "900", hex: "#0F172A" },
        { shade: "800", hex: "#1E293B" },
        { shade: "700", hex: "#334155" },
        { shade: "600", hex: "#475569" },
        { shade: "500", hex: "#64748B" },
        { shade: "400", hex: "#94A3B8" },
        { shade: "300", hex: "#CBD5E1" },
        { shade: "200", hex: "#E2E8F0" },
        { shade: "100", hex: "#F1F5F9" },
        { shade: "50",  hex: "#F8FAFC" },
      ].map(({ shade, hex }) => (
        <div key={shade} className="cp-swatch-wrap">
          <div className="cp-swatch" style={{ background: hex }}>
            <button className="cp-copy-btn" onClick={(e) => { navigator.clipboard.writeText(hex); const b = e.currentTarget; b.classList.add('cp-copy-btn--copied'); setTimeout(() => b.classList.remove('cp-copy-btn--copied'), 1500); }} title={`Copy ${hex}`}>
              <svg className="cp-copy-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><rect x="5" y="5" width="9" height="9" rx="1.5" stroke="currentColor" strokeWidth="1.5"/><path d="M3 11V3a1 1 0 0 1 1-1h8" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round"/></svg>
              <svg className="cp-check-icon" width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8l4 4 6-6" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"/></svg>
            </button>
          </div>
          <span className="cp-swatch-shade">{shade}</span>
          <span className="cp-swatch-hex">{hex}</span>
        </div>
      ))}
    </div>
  </div>

  {/* ─── Dark Surface Colors ──────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Dark Surface Colors</h2>
    <p className="cp-description">These are the designated surface colors for dark mode interfaces. They create depth through subtle layering without relying on pure black.</p>
    <div className="cp-dark-surfaces">
      <div className="cp-dark-card" style={{ background: "#0D0F13", flex: 2 }}>
        <div className="cp-dark-label-group">
          <span className="cp-dark-name">Surface</span>
          <span className="cp-dark-hex">#0D0F13</span>
        </div>
      </div>
      <div style={{ flex: 1, display: "flex", flexDirection: "column", gap: "8px" }}>
        <div className="cp-dark-card" style={{ background: "#1C2136", flex: 1 }}>
          <div className="cp-dark-label-group">
            <span className="cp-dark-name">Surface 02</span>
            <span className="cp-dark-hex">#1C2136</span>
          </div>
        </div>
        <div className="cp-dark-card" style={{ background: "rgba(65,105,225,0.35)", flex: 1, border: "1px solid rgba(65,105,225,0.4)" }}>
          <div className="cp-dark-label-group">
            <span className="cp-dark-name">Accent 35%</span>
            <span className="cp-dark-hex">#4169E1 · 35%</span>
          </div>
        </div>
        <div className="cp-dark-card" style={{ background: "rgba(65,105,225,0.15)", flex: 1, border: "1px solid rgba(65,105,225,0.25)" }}>
          <div className="cp-dark-label-group">
            <span className="cp-dark-name">Accent 15%</span>
            <span className="cp-dark-hex">#4169E1 · 15%</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  {/* ─── Color Tokens ─────────────────────────────────────────────── */}
  <div className="cp-section">
    <h2>Color Tokens</h2>
    <p className="cp-description">Colors are used in the design system through semantic tokens — not raw hex values. Tokens map to specific roles (background, text, border, etc.) and adapt automatically between light and dark modes.</p>
    <div className="cp-tokens-grid">
      {[
        { token: "--color-bg-primary",      value: "#FFFFFF",  label: "Background / Primary",   dark: false },
        { token: "--color-bg-secondary",    value: "#F4F6FE",  label: "Background / Secondary",  dark: false },
        { token: "--color-bg-inverse",      value: "#0D0F13",  label: "Background / Inverse",    dark: true  },
        { token: "--color-text-primary",    value: "#0F172A",  label: "Text / Primary",          dark: false },
        { token: "--color-text-secondary",  value: "#64748B",  label: "Text / Secondary",        dark: false },
        { token: "--color-text-inverse",    value: "#FFFFFF",  label: "Text / Inverse",          dark: true  },
        { token: "--color-brand",           value: "#4169E1",  label: "Brand / Default",         dark: true  },
        { token: "--color-border-default",  value: "#E2E8F0",  label: "Border / Default",        dark: false },
        { token: "--color-border-strong",   value: "#94A3B8",  label: "Border / Strong",         dark: false },
        { token: "--color-success",         value: "#2D9C93",  label: "Semantic / Success",      dark: true  },
        { token: "--color-warning",         value: "#FF9F05",  label: "Semantic / Warning",      dark: false },
        { token: "--color-error",           value: "#D13145",  label: "Semantic / Error",        dark: true  },
      ].map(({ token, value, label, dark }) => (
        <div key={token} className="cp-token-row">
          <div className="cp-token-swatch" style={{ background: value, border: dark ? "none" : "1px solid #E5E7EB" }} />
          <div className="cp-token-info">
            <span className="cp-token-label">{label}</span>
            <code className="cp-token-name">{token}</code>
          </div>
          <span className="cp-token-value">{value}</span>
        </div>
      ))}
    </div>
  </div>

</div>

<style>
  {`
  .cp-page {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 0 64px;
    font-family: inherit;
  }

  .cp-header {
    margin-bottom: 48px;
    padding-bottom: 32px;
    border-bottom: 1px solid #E5E7EB;
  }

  .cp-header h1 {
    font-size: 32px;
    font-weight: 700;
    color: #111827;
    margin: 0 0 12px;
  }

  .cp-header p {
    font-size: 15px;
    color: #6B7280;
    line-height: 1.7;
    max-width: 680px;
    margin: 0;
  }

  .cp-section {
    margin-bottom: 40px;
  }

  .cp-section h2 {
    font-size: 18px;
    font-weight: 700;
    color: #111827;
    margin: 0 0 6px;
  }

  .cp-description {
    font-size: 13px;
    color: #6B7280;
    line-height: 1.7;
    margin: 0 0 20px;
    max-width: 680px;
  }

  /* ── Brand hex row ── */
  .cp-brand-hex-row {
    display: flex;
    gap: 16px;
    margin-bottom: 20px;
    align-items: flex-end;
  }

  .cp-hex-wrap {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .cp-hexagon {
    width: 40px;
    height: 46px;
    display: block;
    flex-shrink: 0;
    overflow: visible;
  }

  .cp-hex-label {
    font-size: 10px;
    color: #9CA3AF;
    text-align: center;
    white-space: nowrap;
  }

  /* ── Brand large cards ── */
  .cp-brand-large-row {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 12px;
  }

  .cp-brand-large-card {
    border-radius: 10px;
    height: 80px;
    padding: 12px 16px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    border: 1px solid rgba(0,0,0,0.06);
    position: relative;
    overflow: hidden;
  }

  .cp-brand-large-card:hover .cp-copy-btn {
    opacity: 1;
  }

  .cp-brand-large-name {
    font-size: 13px;
    font-weight: 600;
  }

  .cp-brand-large-hex {
    font-size: 11px;
    font-family: monospace;
  }

  /* ── Palette row ── */
  .cp-palette-row {
    display: grid;
    grid-template-columns: repeat(10, 1fr);
    gap: 6px;
  }

  .cp-swatch-wrap {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .cp-swatch {
    width: 100%;
    aspect-ratio: 1 / 1.4;
    border-radius: 8px;
    border: 1px solid rgba(0,0,0,0.04);
    min-height: 56px;
    position: relative;
    overflow: hidden;
  }

  .cp-copy-btn {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 0, 0, 0.22);
    border: none;
    cursor: pointer;
    opacity: 0;
    transition: opacity 0.15s ease;
    color: #ffffff;
    padding: 0;
    border-radius: 0;
  }

  .cp-swatch-wrap:hover .cp-copy-btn {
    opacity: 1;
  }

  .cp-copy-btn:active {
    background: rgba(0, 0, 0, 0.35);
  }

  .cp-check-icon {
    display: none;
  }

  .cp-copy-btn--copied .cp-copy-icon {
    display: none;
  }

  .cp-copy-btn--copied .cp-check-icon {
    display: block;
  }

  .cp-swatch-shade {
    font-size: 10px;
    font-weight: 600;
    color: #374151;
  }

  .cp-swatch-hex {
    font-size: 9px;
    color: #9CA3AF;
    font-family: monospace;
    word-break: break-all;
  }

  .cp-swatch-tag {
    font-size: 9px;
    font-weight: 600;
    color: #4169E1;
    background: #ECF0FC;
    padding: 1px 4px;
    border-radius: 3px;
    width: fit-content;
  }

  /* ── Semantic icons ── */
  .cp-semantic-icons {
    display: flex;
    gap: 12px;
    margin-bottom: 20px;
  }

  .cp-semantic-hex {
    width: 48px;
    height: 55px;
    display: block;
    flex-shrink: 0;
  }

  /* ── Dark surfaces ── */
  .cp-dark-surfaces {
    display: flex;
    gap: 12px;
    height: 180px;
  }

  .cp-dark-card {
    border-radius: 12px;
    padding: 16px;
    display: flex;
    align-items: flex-end;
  }

  .cp-dark-label-group {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .cp-dark-name {
    font-size: 12px;
    font-weight: 600;
    color: rgba(255,255,255,0.8);
  }

  .cp-dark-hex {
    font-size: 10px;
    color: rgba(255,255,255,0.45);
    font-family: monospace;
  }

  /* ── Tokens ── */
  .cp-tokens-grid {
    display: flex;
    flex-direction: column;
    border: 1px solid #E5E7EB;
    border-radius: 10px;
    overflow: hidden;
  }

  .cp-token-row {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 10px 16px;
    border-bottom: 1px solid #F3F4F6;
    background: #FFFFFF;
  }

  .cp-token-row:last-child {
    border-bottom: none;
  }

  .cp-token-row:nth-child(even) {
    background: #FAFAFA;
  }

  .cp-token-swatch {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    flex-shrink: 0;
  }

  .cp-token-info {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .cp-token-label {
    font-size: 13px;
    font-weight: 500;
    color: #111827;
  }

  .cp-token-name {
    font-size: 11px;
    color: #6B7280;
    font-family: monospace;
    background: #F3F4F6;
    padding: 1px 5px;
    border-radius: 3px;
    width: fit-content;
  }

  .cp-token-value {
    font-size: 11px;
    color: #9CA3AF;
    font-family: monospace;
    flex-shrink: 0;
  }

  @media (max-width: 700px) {
    .cp-palette-row {
      grid-template-columns: repeat(5, 1fr);
    }
    .cp-brand-large-row {
      grid-template-columns: 1fr;
    }
    .cp-dark-surfaces {
      flex-direction: column;
      height: auto;
    }
    .cp-dark-card {
      min-height: 80px;
    }
  }
  `}
</style>
