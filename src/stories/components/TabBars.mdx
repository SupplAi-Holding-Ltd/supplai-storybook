import { Meta } from "@storybook/addon-docs/blocks";
import React from "react";

<Meta title="Components/Tab Bars" />

export const InteractiveTabs = () => {
  const [active, setActive] = React.useState(0);
  const tabs = ["All Trips", "Allocated", "In Transit", "Cancelled", "Completed"];
  const content = {
    0: { count: 142, color: "#4169E1" },
    1: { count: 38,  color: "#2D9C93" },
    2: { count: 21,  color: "#FF9F05" },
    3: { count: 7,   color: "#D13145" },
    4: { count: 76,  color: "#6B7280" },
  };
  const current = content[active];
  return (
    <div className="tb-live-wrap">
      <div className="tb-live-bar">
        {tabs.map((tab, i) => (
          <button
            key={tab}
            className={`tb-tab ${i === active ? "tb-tab--active" : "tb-tab--inactive"}`}
            onClick={() => setActive(i)}
          >
            {tab}
          </button>
        ))}
      </div>
      <div className="tb-live-panel">
        <div className="tb-live-count" style={{ color: current.color }}>{current.count}</div>
        <div className="tb-live-label">trips in <strong>{tabs[active]}</strong></div>
        <div className="tb-live-rows">
          {Array.from({ length: 4 }).map((_, r) => (
            <div key={r} className="tb-live-row">
              <div className="tb-live-cell tb-live-cell--id" style={{ color: current.color }}>0681-0{r + 1}</div>
              <div className="tb-live-cell">Dagoretti → Nairobi</div>
              <div className="tb-live-cell tb-live-cell--dim">Weekly (Mo, Tu, Fr)</div>
              <div className="tb-live-cell">
                <span className="tb-live-badge" style={{ background: current.color + "20", color: current.color }}>
                  {tabs[active]}
                </span>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};

<div className="tb-page">

  <div className="tb-header">
    <h1>Tab Bars</h1>
    <p>Tab bars are navigation components that allow users to switch between major sections or views within the same page with a single click.</p>
  </div>

  {/* ── Interactive Demo ─────────────────────────────────────────── */}
  <div className="tb-section">
    <h2>Interactive Demo</h2>
    <p className="tb-desc">Click any tab below to switch views. This is how the tab bar behaves in the live application.</p>
    <InteractiveTabs />
  </div>

  {/* ── States ───────────────────────────────────────────────────── */}
  <div className="tb-section">
    <h2>Platform - Tab Navigation</h2>
    <p className="tb-desc">The tab navigation is used extensively in tabular pages. Below are the individual states.</p>

    <p className="tb-sub-desc">Active and Inactive atoms:</p>
    <div className="tb-atoms">
      <div className="tb-atom-wrap">
        <button className="tb-tab tb-tab--active">Active</button>
      </div>
      <div className="tb-atom-wrap">
        <button className="tb-tab tb-tab--inactive">Inactive</button>
      </div>
    </div>

    <p className="tb-sub-desc" style={{ marginTop: "28px" }}>Full states →</p>

    <div className="tb-bar-wrap">
      <div className="tb-bar">
        {["All Trips", "Allocated", "In Transit", "Cancelled", "Completed"].map((label, i) => (
          <button key={label} className={`tb-tab ${i === 0 ? "tb-tab--active" : "tb-tab--inactive"}`}>{label}</button>
        ))}
      </div>
      <span className="tb-state-label">DEFAULT</span>
    </div>

    <div className="tb-bar-wrap">
      <div className="tb-bar">
        {["All Trips", "Allocated", "In Transit", "Cancelled", "Completed"].map((label, i) => (
          <button key={label} className={`tb-tab ${i === 0 ? "tb-tab--active" : i === 2 ? "tb-tab--hover" : "tb-tab--inactive"}`}>
            {label}
          </button>
        ))}
      </div>
      <span className="tb-state-label">HOVER</span>
    </div>

    <div className="tb-bar-wrap">
      <div className="tb-bar">
        {["All Trips", "Allocated", "In Transit", "Cancelled", "Completed"].map((label, i) => (
          <button key={label} className={`tb-tab ${i === 0 ? "tb-tab--active" : i === 2 ? "tb-tab--focused" : "tb-tab--inactive"}`}>
            {label}
          </button>
        ))}
      </div>
      <span className="tb-state-label">FOCUSED</span>
    </div>
  </div>

  {/* ── Usage Guidelines ──────────────────────────────────────────── */}
  <div className="tb-section">
    <h2>Usage Guidelines</h2>
    <div className="tb-guidelines">
      <div className="tb-guideline tb-do">
        <span className="tb-verdict-do">✓ Do</span>
        <ul>
          <li>Use tab bars to switch between parallel views of the same level.</li>
          <li>Keep tab labels short — ideally 1–2 words each.</li>
          <li>Always show one tab as active by default.</li>
          <li>Use the platform style for internal app pages.</li>
        </ul>
      </div>
      <div className="tb-guideline tb-dont">
        <span className="tb-verdict-dont">✕ Don't</span>
        <ul>
          <li>Don't use more than 5–6 tabs — use a dropdown or sidebar instead.</li>
          <li>Don't use tabs to trigger actions — use buttons for that.</li>
          <li>Don't mix tab styles on the same page.</li>
        </ul>
      </div>
    </div>
  </div>

</div>

<style>
  {`
  .tb-page {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 0 64px;
    font-family: 'Manrope', sans-serif;
  }
  .tb-header {
    margin-bottom: 48px;
    padding-bottom: 32px;
    border-bottom: 1px solid #E5E7EB;
  }
  .tb-header h1 { font-size: 32px; font-weight: 700; color: #111827; margin: 0 0 12px; }
  .tb-header p { font-size: 15px; color: #6B7280; line-height: 1.7; max-width: 560px; margin: 0; }
  .tb-section { margin-bottom: 56px; }
  .tb-section h2 { font-size: 20px; font-weight: 700; color: #111827; margin: 0 0 8px; }
  .tb-desc { font-size: 14px; color: #6B7280; line-height: 1.7; margin: 0 0 20px; max-width: 560px; }
  .tb-sub-desc { font-size: 13px; color: #6B7280; margin: 0 0 12px; }

  /* ── Tab base ── */
  .tb-tab {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 6px 14px;
    border-radius: 6px;
    border: 1px solid transparent;
    font-family: 'Manrope', sans-serif;
    font-size: 13px;
    font-weight: 500;
    cursor: pointer;
    transition: background 0.15s, color 0.15s, box-shadow 0.15s;
    white-space: nowrap;
    background: transparent;
  }
  .tb-tab--active {
    background: #FFFFFF;
    border-color: #E5E7EB;
    color: #111827;
    font-weight: 600;
    box-shadow: 0 2px 6px rgba(0,0,0,0.10), 0 1px 2px rgba(0,0,0,0.06);
  }
  .tb-tab--inactive { background: transparent; border-color: transparent; color: #9CA3AF; }
  .tb-tab--hover    { background: #F3F4F6; border-color: transparent; color: #374151; }
  .tb-tab--focused  { background: #EEF2FF; border-color: #4169E1; color: #4169E1; font-weight: 600; box-shadow: 0 0 0 3px rgba(65,105,225,0.18); }

  /* ── Tab bar container ── */
  .tb-bar {
    display: inline-flex;
    align-items: center;
    gap: 2px;
    background: #F9FAFB;
    border: 1px solid #E5E7EB;
    border-radius: 10px;
    padding: 4px;
  }
  .tb-bar-wrap { margin-bottom: 8px; }
  .tb-atoms {
    display: flex;
    gap: 4px;
    background: #F9FAFB;
    border: 1px solid #E5E7EB;
    border-radius: 10px;
    padding: 4px;
    width: fit-content;
    margin-bottom: 8px;
  }
  .tb-atom-wrap { display: flex; flex-direction: column; gap: 6px; }
  .tb-state-label {
    display: block;
    font-size: 9px;
    font-weight: 700;
    color: #9CA3AF;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-top: 6px;
    padding-left: 8px;
  }

  /* ── Interactive live demo ── */
  .tb-live-wrap {
    border: 1px solid #E5E7EB;
    border-radius: 12px;
    overflow: hidden;
    background: #FFFFFF;
  }
  .tb-live-bar {
    display: flex;
    gap: 2px;
    background: #F9FAFB;
    border-bottom: 1px solid #E5E7EB;
    padding: 8px 12px 4px;
  }
  .tb-live-panel {
    padding: 24px;
  }
  .tb-live-count {
    font-size: 36px;
    font-weight: 800;
    line-height: 1;
    margin-bottom: 4px;
  }
  .tb-live-label {
    font-size: 13px;
    color: #6B7280;
    margin-bottom: 20px;
  }
  .tb-live-rows {
    display: flex;
    flex-direction: column;
    gap: 0;
    border: 1px solid #F3F4F6;
    border-radius: 8px;
    overflow: hidden;
  }
  .tb-live-row {
    display: grid;
    grid-template-columns: 80px 200px 1fr 120px;
    padding: 10px 14px;
    border-bottom: 1px solid #F9FAFB;
    align-items: center;
    font-size: 12px;
    background: #FFFFFF;
  }
  .tb-live-row:last-child { border-bottom: none; }
  .tb-live-row:nth-child(even) { background: #FAFAFA; }
  .tb-live-cell { color: #374151; }
  .tb-live-cell--id { font-weight: 600; }
  .tb-live-cell--dim { color: #9CA3AF; }
  .tb-live-badge {
    display: inline-block;
    padding: 2px 8px;
    border-radius: 99px;
    font-size: 11px;
    font-weight: 600;
  }

  /* ── Guidelines ── */
  .tb-guidelines { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
  .tb-guideline { border-radius: 10px; padding: 20px 24px; }
  .tb-do   { background: #F0FDF4; border: 2px solid #BBF7D0; }
  .tb-dont { background: #FFF1F2; border: 2px solid #FECDD3; }
  .tb-verdict-do, .tb-verdict-dont { display: block; font-size: 12px; font-weight: 700; margin-bottom: 12px; }
  .tb-verdict-do   { color: #065F46; }
  .tb-verdict-dont { color: #9F1239; }
  .tb-guideline ul { margin: 0; padding-left: 16px; display: flex; flex-direction: column; gap: 6px; }
  .tb-guideline li { font-size: 13px; line-height: 1.6; color: #374151; }

  @media (max-width: 640px) {
    .tb-guidelines { grid-template-columns: 1fr; }
    .tb-live-row { grid-template-columns: 80px 1fr; }
    .tb-live-row > :nth-child(3), .tb-live-row > :nth-child(4) { display: none; }
  }
  `}
</style>
