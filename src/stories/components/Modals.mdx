import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Components/Modals" />

<div className="sb-container">
  <div className="sb-section-title">
    # Modals

    Modals focus the user's attention on a specific task or piece of information by temporarily blocking interaction with the rest of the page. Use them deliberately — only when an action requires immediate attention.
  </div>

  <div style={{ marginBottom: '40px' }}>
    <h3>Preview</h3>
    <div style={{ position: 'relative', background: '#F9FAFB', borderRadius: '8px', padding: '40px 24px', marginTop: '16px', display: 'flex', alignItems: 'center', justifyContent: 'center', minHeight: '240px' }}>
      <div style={{ position: 'absolute', inset: 0, background: 'rgba(0,0,0,0.4)', borderRadius: '8px' }} />
      <div style={{ position: 'relative', background: '#FFFFFF', borderRadius: '12px', padding: '24px', width: '100%', maxWidth: '400px', boxShadow: '0 20px 60px rgba(0,0,0,0.15)' }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: '12px' }}>
          <h4 style={{ margin: 0, fontSize: '16px', fontWeight: 600, color: '#111827' }}>Confirm Action</h4>
          <span style={{ fontSize: '18px', cursor: 'pointer', color: '#9CA3AF', lineHeight: 1 }}>✕</span>
        </div>
        <p style={{ margin: '0 0 20px', fontSize: '14px', color: '#6B7280', lineHeight: 1.6 }}>
          Are you sure you want to delete this item? This action cannot be undone.
        </p>
        <div style={{ display: 'flex', gap: '8px', justifyContent: 'flex-end' }}>
          <button style={{ padding: '8px 16px', background: '#F3F4F6', border: 'none', borderRadius: '6px', fontSize: '14px', cursor: 'pointer', color: '#374151' }}>Cancel</button>
          <button style={{ padding: '8px 16px', background: '#C81E1E', border: 'none', borderRadius: '6px', fontSize: '14px', cursor: 'pointer', color: '#FFFFFF', fontWeight: 500 }}>Delete</button>
        </div>
      </div>
    </div>
  </div>

  <div style={{ marginBottom: '40px' }}>
    <h3>Usage Guidelines</h3>
    <ul style={{ lineHeight: 2 }}>
      <li>Use modals for tasks that require immediate user action</li>
      <li>Always provide a way to dismiss the modal (close button or Escape key)</li>
      <li>Keep modal content concise — avoid scrollable modals when possible</li>
      <li>Trap focus within the modal while it is open</li>
      <li>Return focus to the triggering element when the modal is closed</li>
    </ul>
  </div>

  <div>
    <h3>Status</h3>
    <p style={{ display: 'inline-flex', alignItems: 'center', gap: '8px', background: '#FEF3C7', color: '#92400E', padding: '6px 12px', borderRadius: '99px', fontSize: '13px', fontWeight: 500 }}>
      🚧 In Development
    </p>
    <p>Interactive stories and full documentation will be added when the component is finalized.</p>
  </div>
</div>

<style>
  {`
  .sb-container { margin-bottom: 48px; }
  .sb-section-title { margin-bottom: 32px; }
  `}
</style>
