import { Meta } from "@storybook/addon-docs/blocks";
import { CodePanel } from "./CodePanel";
import {
  CHECKBOX_CODE,
  CheckboxPlayground,
  SizeDemo,
  CheckedDemo,
  DisabledDemo,
  IndeterminateDemo,
  GroupDemo,
  CustomCardDemo,
  SetupDemo,
  MenuDemo,
  FormDemo,
} from "./Checkboxes";

<Meta title="Components/Checkbox" />

<div className="cb-page">

  <div className="cb-header">
    <h1>Checkbox</h1>
    <p>
      A simple box for selecting one or multiple options in forms, lists, and setting preferences — patterned after{" "}
      <a href="https://radianui.com/docs/components/checkbox" target="_blank" rel="noreferrer">
        Radian UI Checkbox
      </a>
      , using supplai Brand Blue.
    </p>
  </div>

  <div className="cb-section">
    <h2>Live Playground</h2>
    <p className="cb-lede">Toggle size and state. Open the Code tab for an implementation sketch.</p>
    <CodePanel code={CHECKBOX_CODE} filename="Checkbox.tsx" language="tsx">
      <CheckboxPlayground />
    </CodePanel>
  </div>

  <div className="cb-section">
    <h2>Size</h2>
    <p className="cb-lede">Small, medium, and large control sizes.</p>
    <SizeDemo />
  </div>

  <div className="cb-section">
    <h2>Checked</h2>
    <p className="cb-lede">Controlled checked and unchecked states.</p>
    <CheckedDemo />
  </div>

  <div className="cb-section">
    <h2>Disabled</h2>
    <p className="cb-lede">Non-interactive checked and unchecked variants.</p>
    <DisabledDemo />
  </div>

  <div className="cb-section">
    <h2>Indeterminate</h2>
    <p className="cb-lede">Partial selection for parent “select all” controls.</p>
    <IndeterminateDemo />
  </div>

  <div className="cb-section">
    <h2>Checkbox group</h2>
    <p className="cb-lede">Independent multi-select list with clear / submit actions.</p>
    <GroupDemo />
  </div>

  <div className="cb-section">
    <h2>Custom checkbox</h2>
    <p className="cb-lede">Card-style row with title and supporting description.</p>
    <CustomCardDemo />
  </div>

  <div className="cb-section">
    <h2>Checkbox — Setup</h2>
    <p className="cb-lede">Preference list for settings screens.</p>
    <SetupDemo />
  </div>

  <div className="cb-section">
    <h2>Checkbox — Dropdown menu</h2>
    <p className="cb-lede">Multi-select items inside a compact menu panel.</p>
    <MenuDemo />
  </div>

  <div className="cb-section">
    <h2>Form</h2>
    <p className="cb-lede">Grouped options with select-all, terms, and submit.</p>
    <FormDemo />
  </div>

  <div className="cb-section">
    <h2>Specs</h2>
    <div className="cb-specs-grid">
      <div className="cb-spec-card">
        <span className="cb-spec-label">Sizes</span>
        <span className="cb-spec-value">sm 14 · md 16 · lg 20</span>
      </div>
      <div className="cb-spec-card">
        <span className="cb-spec-label">Radius</span>
        <span className="cb-spec-value">4–6px</span>
      </div>
      <div className="cb-spec-card">
        <span className="cb-spec-label">Accent</span>
        <span className="cb-spec-value">Brand Blue #4169E1</span>
      </div>
      <div className="cb-spec-card">
        <span className="cb-spec-label">States</span>
        <span className="cb-spec-value">unchecked · checked · indeterminate · disabled</span>
      </div>
      <div className="cb-spec-card">
        <span className="cb-spec-label">Input</span>
        <span className="cb-spec-value">Native checkbox + custom mark</span>
      </div>
      <div className="cb-spec-card">
        <span className="cb-spec-label">A11y</span>
        <span className="cb-spec-value">Label association · focus ring · Space toggle</span>
      </div>
    </div>
  </div>

</div>
