import { Meta } from "@storybook/addon-docs/blocks";
import { CodePanel } from "./CodePanel";
import {
  STEPPER_CODE,
  StepperPlayground,
  DefaultDemo,
  CompleteStatesDemo,
  ControlButtonsDemo,
  TitleDemo,
  DescriptionDemo,
  IconDemo,
  ProgressBarDemo,
  VerticalDemo,
  ControlledDemo,
  UncontrolledDemo,
  LinearDemo,
  NonLinearDemo,
  DisabledDemo,
  LoadingDemo,
  ErrorStepDemo,
  FormDemo,
  CompletionDemo,
  CompactMobileDemo,
  DynamicStepsDemo,
  ForceMountDemo,
  AnatomyDemo,
  CompareDemo,
  SpecsDemo,
} from "./Steppers";

<Meta title="Components/Stepper" />

<div className="stp-page">

  <div className="stp-header">
    <h1>Stepper</h1>
    <p>
      A step-by-step progress guide for multi-stage workflows — checkout, onboarding, and
      multi-step forms. Compose indicators, titles, connectors, and panels; keep validation and
      Previous/Next controls in the application.
    </p>
  </div>

  <div className="stp-section">
    <h2>Live Playground</h2>
    <p className="stp-lede">Orientation and current step.</p>
    <CodePanel code={STEPPER_CODE} filename="Stepper.tsx" language="tsx">
      <StepperPlayground />
    </CodePanel>
  </div>

  <div className="stp-section">
    <h2>Default</h2>
    <p className="stp-lede">Interactive horizontal Stepper with step content beneath the nav.</p>
    <DefaultDemo />
  </div>

  <div className="stp-section">
    <h2>Complete states</h2>
    <p className="stp-lede">
      Steps before the current value render as completed (check indicator + success connector).
    </p>
    <CompleteStatesDemo />
  </div>

  <div className="stp-section">
    <h2>With control buttons</h2>
    <p className="stp-lede">
      Previous / Next are composition using design-system buttons — not built into Stepper.
    </p>
    <ControlButtonsDemo />
  </div>

  <div className="stp-section">
    <h2>With title</h2>
    <TitleDemo />
  </div>

  <div className="stp-section">
    <h2>With description</h2>
    <p className="stp-lede">Supporting text uses muted typography; it may hide on narrow widths.</p>
    <DescriptionDemo />
  </div>

  <div className="stp-section">
    <h2>With icon</h2>
    <p className="stp-lede">
      Indicators accept icons. Titles (or accessible labels) must remain — icons alone are not
      enough.
    </p>
    <IconDemo />
  </div>

  <div className="stp-section">
    <h2>With Progress bar</h2>
    <p className="stp-lede">
      Progress percentage is derived from the active step — keep Stepper and Progress synchronised.
    </p>
    <ProgressBarDemo />
  </div>

  <div className="stp-section">
    <h2>Vertical</h2>
    <p className="stp-lede">
      <code>orientation=&quot;vertical&quot;</code> — connectors stack; content sits beside the nav on
      wide layouts.
    </p>
    <VerticalDemo />
  </div>

  <div className="stp-section">
    <h2>Controlled</h2>
    <p className="stp-lede">
      <code>value</code> + <code>onValueChange</code> keep external state, indicators, and content
      in sync.
    </p>
    <ControlledDemo />
  </div>

  <div className="stp-section">
    <h2>Uncontrolled</h2>
    <UncontrolledDemo />
  </div>

  <div className="stp-section">
    <h2>Linear workflow</h2>
    <p className="stp-lede">
      Lock future steps with <code>disabled</code> until the user advances. Suitable for checkout
      and required multi-step forms.
    </p>
    <LinearDemo />
  </div>

  <div className="stp-section">
    <h2>Non-linear</h2>
    <p className="stp-lede">Any unlocked step is clickable — settings and editable sections.</p>
    <NonLinearDemo />
  </div>

  <div className="stp-section">
    <h2>Disabled step</h2>
    <p className="stp-lede">
      Disabled is not the same as inactive. Unavailable steps do not respond to pointer or keyboard
      activation.
    </p>
    <DisabledDemo />
  </div>

  <div className="stp-section">
    <h2>Loading step</h2>
    <p className="stp-lede">
      Reuses Spinner via <code>indicators.loading</code>. Surrounding status text announces the
      work.
    </p>
    <LoadingDemo />
  </div>

  <div className="stp-section">
    <h2>Error composition</h2>
    <p className="stp-lede">
      Error is usually content-level — compose Alert treatment rather than a core Stepper{" "}
      <code>error</code> prop.
    </p>
    <ErrorStepDemo />
  </div>

  <div className="stp-section">
    <h2>Multi-step form</h2>
    <p className="stp-lede">
      Validate the current step before advancing. Values persist when navigating backward (
      <code>forceMount</code> on form panels). No React Hook Form dependency — use your product’s
      form stack.
    </p>
    <FormDemo />
  </div>

  <div className="stp-section">
    <h2>Completion screen</h2>
    <p className="stp-lede">
      Distinguish “step 4 is current” from “the whole process finished” — use a dedicated success
      state.
    </p>
    <CompletionDemo />
  </div>

  <div className="stp-section">
    <h2>Compact mobile header</h2>
    <p className="stp-lede">
      Recommended narrow-layout composition: step count, title, and Progress — not a{" "}
      <code>mobileMode</code> prop.
    </p>
    <CompactMobileDemo />
  </div>

  <div className="stp-section">
    <h2>Dynamic steps</h2>
    <DynamicStepsDemo />
  </div>

  <div className="stp-section">
    <h2>forceMount</h2>
    <ForceMountDemo />
  </div>

  <div className="stp-section">
    <h2>Installation</h2>
    <pre
      className="stp-note"
      style={{
        margin: 0,
        padding: "14px 16px",
        borderRadius: 10,
        border: "1px solid #E2E8F0",
        background: "#F8FAFC",
        fontFamily: "JetBrains Mono, ui-monospace, monospace",
        fontSize: 12,
        color: "#0F172A",
        overflow: "auto",
      }}
    >
{`import {
  Stepper,
  StepperNav,
  StepperItem,
  StepperTrigger,
  StepperIndicator,
  StepperSeparator,
  StepperTitle,
  StepperDescription,
  StepperPanel,
  StepperContent,
} from './Stepper';`}
    </pre>
  </div>

  <div className="stp-section">
    <h2>Component anatomy</h2>
    <AnatomyDemo />
  </div>

  <div className="stp-section">
    <h2>Component API</h2>
    <h3 style={{ fontSize: 14, margin: "0 0 8px", color: "#0F172A" }}>Stepper</h3>
    <table className="stp-api-table">
      <thead>
        <tr>
          <th>Prop</th>
          <th>Type</th>
          <th>Default</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>defaultValue</code></td>
          <td>number</td>
          <td>1</td>
          <td>Initial step when uncontrolled</td>
        </tr>
        <tr>
          <td><code>value</code></td>
          <td>number</td>
          <td>—</td>
          <td>Controlled current step</td>
        </tr>
        <tr>
          <td><code>onValueChange</code></td>
          <td>(value: number) =&gt; void</td>
          <td>—</td>
          <td>Called when the step changes</td>
        </tr>
        <tr>
          <td><code>orientation</code></td>
          <td>&quot;horizontal&quot; | &quot;vertical&quot;</td>
          <td>horizontal</td>
          <td>Layout and connector direction</td>
        </tr>
        <tr>
          <td><code>indicators</code></td>
          <td>&#123; active?, completed?, inactive?, loading? &#125;</td>
          <td>—</td>
          <td>Optional indicator overrides by state</td>
        </tr>
        <tr>
          <td><code>className</code></td>
          <td>string</td>
          <td>—</td>
          <td>Extra class on the root</td>
        </tr>
      </tbody>
    </table>

    <h3 style={{ fontSize: 14, margin: "20px 0 8px", color: "#0F172A" }}>StepperItem</h3>
    <table className="stp-api-table">
      <thead>
        <tr>
          <th>Prop</th>
          <th>Type</th>
          <th>Default</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>step</code></td>
          <td>number</td>
          <td>required</td>
          <td>Step identifier</td>
        </tr>
        <tr>
          <td><code>completed</code></td>
          <td>boolean</td>
          <td>false</td>
          <td>Force completed (also auto when step &lt; active)</td>
        </tr>
        <tr>
          <td><code>disabled</code></td>
          <td>boolean</td>
          <td>false</td>
          <td>Locks the step (linear / unavailable)</td>
        </tr>
        <tr>
          <td><code>loading</code></td>
          <td>boolean</td>
          <td>false</td>
          <td>Shows loading indicator when this step is active</td>
        </tr>
      </tbody>
    </table>

    <h3 style={{ fontSize: 14, margin: "20px 0 8px", color: "#0F172A" }}>StepperContent</h3>
    <table className="stp-api-table">
      <thead>
        <tr>
          <th>Prop</th>
          <th>Type</th>
          <th>Default</th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><code>value</code></td>
          <td>number</td>
          <td>required</td>
          <td>Matches Stepper active step</td>
        </tr>
        <tr>
          <td><code>forceMount</code></td>
          <td>boolean</td>
          <td>false</td>
          <td>Keep mounted when inactive (hidden + aria-hidden)</td>
        </tr>
      </tbody>
    </table>

    <p className="stp-lede" style={{ marginTop: 12 }}>
      <code>StepperTrigger</code>, <code>StepperIndicator</code>, <code>StepperSeparator</code>,{" "}
      <code>StepperTitle</code>, <code>StepperDescription</code>, <code>StepperNav</code>, and{" "}
      <code>StepperPanel</code> accept <code>className</code> and standard HTML attributes. Trigger
      supports optional <code>asChild</code>.
    </p>
  </div>

  <div className="stp-section">
    <h2>Stepper vs Progress vs Tabs vs Breadcrumb</h2>
    <CompareDemo />
  </div>

  <div className="stp-section">
    <h2>Usage guidelines</h2>
    <div className="stp-guidelines">
      <div className="stp-guideline stp-guideline--do">
        <span className="stp-gl-tag stp-gl-tag--do">Do</span>
        <ul>
          <li>Use for named workflow stages with clear progression</li>
          <li>Communicate state with indicator + label + connector together</li>
          <li>Validate before advancing in linear forms</li>
          <li>Compose Progress / Buttons / Spinner — don’t fork them into Stepper</li>
        </ul>
      </div>
      <div className="stp-guideline stp-guideline--dont">
        <span className="stp-gl-tag stp-gl-tag--dont">Don&apos;t</span>
        <ul>
          <li>Don&apos;t use Stepper for peer navigation (use Tabs)</li>
          <li>Don&apos;t rely on colour alone for completed / current / disabled</li>
          <li>Don&apos;t treat “last step active” as process complete</li>
          <li>Don&apos;t auto-switch orientation unless product layout requires it</li>
        </ul>
      </div>
    </div>
  </div>

  <div className="stp-section">
    <h2>Accessibility</h2>
    <ul style={{ margin: 0, paddingLeft: 18, fontSize: 13, lineHeight: 1.6, color: "#475569" }}>
      <li>
        Nav uses <code>aria-label=&quot;Progress&quot;</code> and announces{" "}
        <code>Step N of M</code>
      </li>
      <li>
        Current trigger sets <code>aria-current=&quot;step&quot;</code>
      </li>
      <li>Visually hidden status appends completed / loading / unavailable / current</li>
      <li>Arrow keys move focus between triggers; Home / End jump to ends; Enter / Space activate</li>
      <li>Disabled steps are not activatable</li>
      <li>
        Inactive <code>forceMount</code> content stays <code>hidden</code> and{" "}
        <code>aria-hidden</code>
      </li>
      <li>Loading: announce via visible status text; set Spinner <code>announce=&#123;false&#125;</code></li>
    </ul>
  </div>

  <div className="stp-section">
    <h2>Specs</h2>
    <SpecsDemo />
  </div>

</div>
